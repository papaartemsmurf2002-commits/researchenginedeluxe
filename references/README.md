# References

This directory keeps only source material required for LC parity reasoning.

- `lorentzian-classification/original_lorentzian_classification.pine`: original TradingView LC source snapshot.
- `lorentzian-classification/settings.txt`: baseline settings snapshot used by `default_app_config()`.
- `pine-libraries/`: source library snapshots used to port feature and kernel helpers.

Do not add generated TradingView CSV exports here.
