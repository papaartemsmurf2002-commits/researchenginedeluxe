# Runtime Operator Guide Pointer

The canonical current operator guide is `docs/OPERATOR_GUIDE.md`.

This runtime copy is kept only for older references under
`docs/tradingbotsuite_runtime/`. The operator UI now prefers the canonical
root-level docs so the embedded Guides page stays aligned with the current
Research page, live-boundary rules, discovery controls, and Stage 13 readiness
diagnostics.

Use these files first:

- `docs/OPERATOR_QUICKSTART.md` for the compact daily run card.
- `docs/OPERATOR_GUIDE.md` for the full browser console, research page,
  live/testnet, safety, and troubleshooting reference.
- `docs/REPO_STRUCTURE_AND_DEPENDENCY_FUSE.md` before changing shared
  research, feature, backtest, data, artifact, or UI infrastructure.
